# The simple fixtures as a deck

Every card note in this folder, on A4 in whichever orientation holds more
cards, cut marks on. The block says nothing it does not have to: the folder
is this note's own, the system's one card type needs no `card-type:`.

```card-forge-deck
system: simple
```
