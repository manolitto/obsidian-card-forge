{{!--
  The One Ring — ONE key number: its caption over a diamond.

      {{> diamond for="front-damage"}}

  The caption is the place's from the translations; the number sits in a
  rotated square and shrinks only when it would not fit. The caller
  decides whether the place is shown at all.
--}}
<div class="tor-stat"><div class="tor-stat-label">{{slot-label for}}</div><div class="tor-diamond"><div class="tor-diamond-inner"><span class="text-scalable">{{slot for markdown=false}}</span></div></div></div>