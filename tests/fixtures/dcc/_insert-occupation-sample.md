```cardsmith
card:
  system: dcc
  card-type: occupation
data:
  # The card's name. Falls back to the file name.
  name: Stargazer

  # Flavour or a short description, in italics.
  description: # no sample

  # The roll the entry is listed under in its table — a badge beside the title. `roll-min` and `roll-max` beside it are the numbers.
  roll: '07'

  # Source reference — rulebook page or wikilink, small at the foot's right.
  reference: Core Rulebook p. 22

  # English original name, small at the foot's left, prefixed "engl."; hidden on English cards.
  original-name: Stargazer

  # The people of a demi-human occupation — Halfling, Dwarf, Elf. Only where one applies.
  race: # no sample

  # The weapon the occupation is trained in, with its die — "Staff (1d4)".
  trained-weapon: Staff (1d4)

  # The trade goods the occupation starts with.
  trade-goods: Star chart
```
