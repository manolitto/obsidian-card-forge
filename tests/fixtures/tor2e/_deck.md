# The One Ring fixtures as a deck

Every card note in this folder. The adversary prints `large` on its own;
a deck is one grid, so `card-size:` here puts everything on poker.

```card-forge-deck
system: tor2e
card-type: [npc, gear]
card-size: poker
paper-size: A4 portrait
```
